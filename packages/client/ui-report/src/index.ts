/**
 * Web display-zone gallery plugin, node half.
 *
 * Deliberately empty. The report service lives on the host plane and the
 * gallery is a pure browser surface; the browser half below owns the UI.
 */

/** Host plugin body — the host report service is composed in the base bundle, not here. */
export function apply(): void {}
