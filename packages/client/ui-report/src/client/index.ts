/**
 * Web display-zone gallery plugin, browser half: the report list and tag
 * sidebar. The gallery consumes ctx.reports through the host API surface;
 * this skeleton registers the plugin entry with no slots yet.
 *
 * @module @deepseek-ai/dsh-client-ui-report/client
 */

/** Required service: the slot registry. */
export const inject = ['slots']

/**
 * Client plugin body. Skeleton: registers no slots yet; the report list and
 * tag sidebar land here once the host report API is exposed to the browser.
 */
export function apply(): void {}
